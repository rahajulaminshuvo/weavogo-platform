{
  "//": "Template. Copy the shape, never the values. Real secrets live in user-secrets or environment variables - see README.",

  "ConnectionStrings": {
    "//DefaultConnection": "Set via: dotnet user-secrets set \"ConnectionStrings:DefaultConnection\" \"<value>\"",
    "DefaultConnection": "Server=<host>,1433;Database=<db>;User Id=<user>;Password=<password>;TrustServerCertificate=True;Connect Timeout=60"
  },

  "JwtSettings": {
    "//Secret": "Set via: dotnet user-secrets set \"JwtSettings:Secret\" \"<32+ char key>\". Required outside Development; start-up fails without it.",
    "Issuer": "ItemMaster.Api",
    "Audience": "ItemMaster.Clients"
  }
}
